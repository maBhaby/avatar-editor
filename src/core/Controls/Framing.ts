import Editor from "../Editor"

class Framing {
  private _editor: Editor

  constructor(editor: Editor) {
    this._editor = editor
    this._initControls()
  }

  private _initControls = () => {
    const canvas = this._editor.getMainContainer()

    
  }
}

export default Framing 
